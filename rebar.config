%% Normally I would put this in, but erlzmq is not rebar-compatible.
%% Might be better to rewrite the code to use erlzmq2.
%% {deps, [{zmq, ".*", {git, "git://github.com/zeromq/erlzmq.git", {branch, "master"}}}]}.
